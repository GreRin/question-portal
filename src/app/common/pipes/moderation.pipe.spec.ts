import { ModerationPipe } from './moderation.pipe';

describe('ModerationPipe', () => {
  it('create an instance', () => {
    const pipe = new ModerationPipe();
    expect(pipe).toBeTruthy();
  });
});
